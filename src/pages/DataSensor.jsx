import React, { useState } from 'react';
import SensorTable from '../components/SensorTable';

const DataSensor = () => {
    return (
        <div className="animated-enter">
            <h1 className="heading-card">Data Sensor</h1>
            <SensorTable />
        </div>
    );
}

export default DataSensor;
